//

let array = [1,2,3,[1,2,3,[1,2,3]]];

console.log(array.flat(2)); // flat: recibecomo argumento lapprofuncidad(1 nivel)

let arra = [1,2,3,4,5];

//mapear valores
console.log(arra.flatMap(value => [value, value * 2]));

// mapear esaciosen blanc ode un string

console.log(hello);
consol.log(hello.trimStar());

let hello = 'hello world         ';
console.log(hello);
consol.log(hello.trimEnd());


// pasar de forma opcionona al valor de catch

try{
} catch {
error
}


// fromEntries: pasar de valores a arreglos o dearegls a valores

let entries =[["name", "oscar"], ["age", 32]];
console.log(Object.fromEntrues(entries));


// objeto de tipo simblo, ver elementos

let mySymbol = 'My symbol';
let symbol = Symbol(mySymbol);
console.log(symbol.description);